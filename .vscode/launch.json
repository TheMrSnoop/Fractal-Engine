{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with MSVC",
            "type": "cppvsdbg", // <-- Switched to the Visual Studio Debugger
            "request": "launch",
            "program": "${workspaceFolder}/FractalEngine.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "preLaunchTask": "Build with MSVC (Correct Path)" // <-- Matches the new task label
        }
    ]
}