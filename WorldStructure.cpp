#include"WorldStructure.h"
#include <iostream>
#include"Block.h"

WorldStructure::WorldStructure(std::string StructureID, Block block[], std::vector<glm::vec3> relativePos[])
{
	if (StructureID == "Ruins")
	{
		//StructureParts[0].block = Block::Block();
	}
}